<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0"
           xsi:schemaLocation="
             http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0 http://aries.apache.org/schemas/blueprint-cm/blueprint-cm-1.1.0.xsd
             http://www.osgi.org/xmlns/blueprint/v1.0.0 https://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd">

    <bean id="mqttClientCloud" class="br.uefs.larsid.dlt.iot.soft.mqtt.MQTTClient"
        init-method="start" destroy-method="stop">
        <property name="debugModeValue" value="${debugModeValue}" />
        <property name="ip" value="${ip_cloud}" />
        <property name="port" value="${port_cloud}" />
        <property name="userName" value="${user_cloud}" />
        <property name="password" value="${pass_cloud}" />
    </bean>

    <bean id="mqttClientFogBroker" class="br.uefs.larsid.dlt.iot.soft.mqtt.MQTTClient"
        init-method="start" destroy-method="stop">
        <property name="debugModeValue" value="${debugModeValue}" />
        <property name="ip" value="${ip_fog}" />
        <property name="port" value="${port_fog}" />
        <property name="userName" value="${user_fog}" />
        <property name="password" value="${pass_fog}" />
    </bean>

    <bean id="mqttClientBottomBroker" class="br.uefs.larsid.dlt.iot.soft.mqtt.MQTTClient"
        init-method="start" destroy-method="stop">
        <property name="debugModeValue" value="${debugModeValue}" />
        <property name="ip" value="${ip_bottom}" />
        <property name="port" value="${port_bottom}" />
        <property name="userName" value="${user_bottom}" />
        <property name="password" value="${pass_bottom}" />
    </bean>

    <bean id="entitiesController" class="br.uefs.larsid.dlt.iot.soft.model.ControllerImpl"
        init-method="start" destroy-method="stop">
        <property name="debugModeValue" value="${debugModeValue}" />
        <property name="mqttClientBottomBroker" ref="mqttClientBottomBroker" />
        <property name="mqttClientFogBroker" ref="mqttClientFogBroker" />
        <property name="mqttClientCloud" ref="mqttClientCloud" />
        <property name="childs" value="${childs}" />
    </bean>

  <cm:property-placeholder
		persistent-id="br.uefs.larsid.dlt.soft_iot.fog_broker"
		update-strategy="reload">
		<cm:default-properties>
			<cm:property name="ip_cloud" value="localhost" />
			<cm:property name="port_cloud" value="1883" />
			<cm:property name="user_cloud" value="karaf" />
			<cm:property name="pass_cloud" value="karaf" />
			<cm:property name="ip_fog" value="localhost" />
			<cm:property name="port_fog" value="1883" />
			<cm:property name="user_fog" value="karaf" />
			<cm:property name="pass_fog" value="karaf" />
			<cm:property name="ip_bottom" value="localhost" />
			<cm:property name="port_bottom" value="1883" />
			<cm:property name="user_bottom" value="karaf" />
			<cm:property name="pass_bottom" value="karaf" />
			<cm:property name="debugModeValue" value="true" />
			<cm:property name="childs" value="0" />
		</cm:default-properties>
	</cm:property-placeholder>

</blueprint>